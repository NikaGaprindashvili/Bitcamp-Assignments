# Asks the person to write something, person writes something, it outputs person's written string using "..." as separator

playback = input("")
playback = playback.replace(" ", "...")

print (playback)

